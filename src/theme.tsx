import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: { default: "#f9f9f9" },
    primary: {
      main: "#111",
    },
    secondary: {
      main: "#fff",
    },
  },
});

export { theme };
